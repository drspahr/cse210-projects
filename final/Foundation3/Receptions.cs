class Receptions : Events
{
    private bool _register;

    // Constructor
    public Receptions(string title, string desc, string date, string time) : base(title, desc, date, time)
    {

    }

    // Method
    public bool IsRegister()
    {
        bool reg = false;
        return  reg;
    }
}